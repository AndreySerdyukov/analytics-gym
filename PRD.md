# PRD — analytics-gym

Что приложение делает с точки зрения поведения. Технические конвенции — в [`CLAUDE.md`](CLAUDE.md).

## Проблема
Задачи с собеседований и теория по DA/DS не собраны в одном месте: часть в голове, часть в
закладках. Повторять нечего, прогресс не виден, SQL-задачи решаются «на глаз» без проверки.

## Пользователь
Один человек — владелец репозитория. Готовится к собеседованиям на Data Analyst / Data Scientist,
пополняет базу задач сам, занимается регулярно короткими сессиями.

## Ключевые сценарии

### 1. Записать задачу, которая встретилась
Задача создаётся командой `new-task`, дописывается в редакторе, проверяется `validate` и попадает
в приложение после `sync`. Трение минимальное: не нужно открывать UI и заполнять форму.

### 2. Порешать SQL с автопроверкой
Открыть блок SQL, отфильтровать по сложности или теме, открыть задачу, написать запрос, запустить.
Приложение выполняет запрос пользователя и эталон на одном датасете и сравнивает результаты.
При расхождении показывает, чем результаты отличаются, а не просто «неверно».

### 3. Повторить теорию
На главной видно, сколько карточек к повторению сегодня. Режим `/review` показывает вопрос,
по кнопке — ответ, дальше самооценка (забыл / с трудом / легко). Алгоритм SM-2 назначает
следующую дату показа.

### 4. Понять, где слабые места
Статистика показывает решаемость по блокам и тегам, среднее время, историю занятий.

## Требования по блокам

### Блоки и навигация
- Блоки первой версии: `sql`, `stats-ab`, `python`, `ml`. Набор задаётся в `content/blocks.yaml`,
  добавление нового блока не требует изменений кода.
- Переключение между блоками доступно с любой страницы. Вкладка блока в шапке остаётся активной
  и на вложенных адресах: и в теории блока, и в его практике.
- Блок — это хаб с выбором: теория или практика. У каждой половины виден свой прогресс
  («прочитано N из M», «решено N из M»).
- Пустая половина хаба не ссылка, а приглушённая карточка с подписью «пока пусто»: клик уводил бы
  на пустой экран без пути назад. Сейчас задачи есть только в `sql`, конспекты — в `sql`,
  `stats-ab` и `python`.
- Внутри практики задачи группируются по темам (`topic`), внутри теории — тоже.

### Задачи
- Поля: заголовок, блок, тема, сложность (`easy|medium|hard`), теги, источник
  (`interview|course|leetcode|own`), компания, оценка времени, датасет, настройки проверки.
- Разделы: условие, эталонное решение, разбор.
- Решение и разбор по умолчанию скрыты и раскрываются явным действием.
- Статусы: `new` → `in_progress` (была хотя бы одна попытка) → `solved` (была верная попытка).
  Статус `failed` выставляется вручную пользователем, если он сдался.
- К задаче можно написать личную заметку. Она хранится только в БД и не попадает в репозиторий.

### SQL-раннер
- Движок — PGlite (Postgres в WASM), выполняется в браузере, в воркере.
- Перед каждой задачей схема сбрасывается и заново накатывается датасет: результат не зависит
  от предыдущих запусков и от того, что пользователь выполнял `DROP TABLE`.
- Запрос пользователя выполняется в транзакции с откатом: `INSERT`/`UPDATE`/`DROP` в запросе не
  портят датасет для следующей попытки.
- Ограничения: таймаут выполнения (по умолчанию 10 секунд, воркер убивается), лимит отображаемых
  строк (по умолчанию 200, с пометкой об усечении).
- Ошибка Postgres показывается как есть — это часть обучения.

### Сравнение результатов
- Ожидаемый результат не хранится: эталонный SQL выполняется на том же датасете при каждой проверке.
- Настройки в блоке `check` задачи:
  - `ordered` (по умолчанию `false`) — если `false`, строки сравниваются как мультимножество;
  - `tolerance` (по умолчанию `0.001`) — допуск при сравнении чисел;
  - `ignore_column_names` (по умолчанию `true`) — сравнивать по позициям колонок, а не по именам.
- Edge cases: разное число колонок → «ожидалось N колонок, получено M»; разное число строк →
  показать сколько лишних и недостающих; `NULL` считается равным `NULL`; числовые типы
  сравниваются как числа независимо от того, вернулись они как `numeric` или `float`.

### Теория
Теория живёт внутри блока, а не отдельным общим разделом: искать конспект по Python, уходя
из блока Python в сводный список, — лишний шаг, а практика и теория одной темы должны лежать рядом.

- Конспект — Markdown-файл с содержательными разделами и разделом «Карточки».
- Слева список тем блока в порядке `blocks.yaml`, внутри темы — конспекты в порядке номеров файлов.
  Тема без конспектов в списке не показывается: пустой скелет оглавления только шумит, а полный
  набор тем виден на хабе блока и на дашборде.
- Конспекты без темы и конспекты с темой, которой в блоке уже нет, собираются в группу «Без темы»
  в конце списка. Потерять конспект из навигации нельзя: раз файл в `content/` есть, он должен быть виден.
- На узком экране боковой список сворачивается в «Содержание» над текстом.
- На `/b/:blockSlug/theory` без выбранного конспекта показывается вводный экран: сколько конспектов
  и карточек, сколько прочитано, темы блока и кнопка «Продолжить» на первый непрочитанный конспект.
  Автоперехода на первый конспект нет намеренно: он менял бы адрес под пользователем.
- Конспект отмечается прочитанным явным действием — кнопкой «Прочитано, дальше →» внизу текста,
  которая ставит отметку и открывает следующую тему по порядку списка. У последней темы блока
  надпись «Прочитано, блок пройден», и она возвращает на хаб.
- Отметку можно снять. Снятие очищает дату прочтения: «прочитал, потом передумал» не должно
  оставлять в истории дату, которой уже ничего не соответствует.
- Прочитанные темы помечены галочкой в боковом списке; счётчик «прочитано N из M» виден в самом
  списке, на хабе блока и на карточке блока на дашборде.
- Чтение конспекта **не** попадает в календарь занятий: календарь строится по попыткам и
  повторениям и означает «решал или повторял».
- Вопросы из раздела «Карточки» автоматически попадают в режим повторения после `sync`.
- В тексте конспекта допустим HTML-спойлер `<details><summary>…</summary>`: он нужен разделу
  «Проверь себя», где ответы должны открываться только после попытки вспомнить.
- Edge cases: адрес конспекта из чужого блока (`/b/ml/theory/sql-…`) переадресуется на правильный
  блок; несуществующий слаг конспекта показывает ошибку со ссылкой на список, а боковой список
  остаётся на месте; блок без конспектов показывает подсказку с командой `new-note`.

### Повторение (SM-2)
- Карточки берутся из раздела `## Карточки` конспектов теории.
- Оценки: `0` забыл, `3` с трудом, `5` легко. Промежуточных значений нет намеренно — три кнопки
  вместо пяти, чтобы не тратить внимание на выбор оценки.
- Первый интервал 1 день, второй 6 дней, дальше умножается на ease factor (минимум 1.3).
- Оценка ниже 3 сбрасывает счётчик повторений, интервал возвращается к 1 дню. Ease factor при
  этом всё равно снижается: карточка, которую забывают раз за разом, растёт в интервалах медленнее.
- К повторению сегодня = карточки с `due_date <= сегодня` плюс никогда не показанные.
  Порядок — от самых просроченных к новым.
- Ответ скрыт до явного действия: сначала вспоминаешь сам, потом сверяешься.
- Управление с клавиатуры: пробел раскрывает ответ, цифры 1-3 ставят оценку.
- После оценки показывается, когда карточка вернётся («завтра», «через 6 дней»).
- Счётчик карточек на сегодня виден в шапке на всех страницах и на дашборде.
- Edge case: если карточка удалена из контента, её история повторений сохраняется, но карточка
  больше не показывается.

### Статистика
Считается по фактам — попыткам и повторениям, а не по самооценке.

- **Сводка:** решено задач, точность попыток, закреплено карточек (от трёх успешных повторений
  подряд), текущая серия занятий.
- **Календарь занятий** за 91 день: день — ячейка, интенсивность — число действий. Точные числа
  доступны по наведению, а не только цветом.
- **Прогресс по блокам:** решено из скольких, число попыток, среднее время верного решения.
- **Где спотыкаюсь:** темы, отсортированные по числу неудачных попыток. Именно неудачных попыток,
  а не нерешённых задач: важно, где реально спотыкаешься, а не чего просто много.
- Серия занятий: сегодняшний день без активности серию не обрывает — день ещё не закончился,
  поэтому отсчёт в этом случае начинается со вчера.
- Edge case: пока нет ни одной попытки, раздел «Где спотыкаюсь» показывает подсказку вместо
  пустых полос. Если у всех тем одинаковое число неудач, полосы не рисуются вовсе — сравнивать
  нечего, достаточно чисел.

## Маршруты

| Маршрут | Что показывает |
|---|---|
| `/` | Дашборд: блоки с прогрессом по теории и практике, незаконченные задачи |
| `/b/:blockSlug` | Хаб блока: выбор между теорией и практикой |
| `/b/:blockSlug/theory` | Теория блока: темы слева, вводный экран справа |
| `/b/:blockSlug/theory/:noteSlug` | Один конспект с тем же списком тем слева |
| `/b/:blockSlug/practice` | Список задач блока с фильтрами и поиском |
| `/t/:taskSlug` | Задача: условие, схема данных, редактор, решение, разбор, заметка |
| `/review` | Режим повторения SM-2 по карточкам всех блоков |
| `/stats` | Статистика: прогресс, календарь занятий, слабые темы |

Слаг задачи глобально уникален и включает блок (`sql-retention-d7`), поэтому блок в адресе
задачи не дублируется.

Старые адреса теории сохранены как редиректы: `/theory` уводит на дашборд, `/theory/:noteSlug` —
на блочный адрес конспекта. Блок при этом спрашивается у источника данных, а не вычисляется
разбором слага: слаг устроен как `<блок>-<имя файла>`, а `stats-ab` сам содержит дефис, поэтому
`stats-ab-proverka-gipotez` разбирается неоднозначно.

## Режим публичного демо
Тот же интерфейс, собранный статически для GitHub Pages:
- контент читается из сгенерированного `content.json`;
- прогресс, попытки, SRS и отметки о прочтении конспектов хранятся в `localStorage` браузера
  посетителя (ключи `gym-progress`, `gym-review`, `gym-activity`, `gym-notes-read`);
- SQL-раннер работает полностью (он и так в браузере);
- личные заметки в демо не публикуются;
- маршруты живут в хеше (`/#/t/slug`): Pages отдаёт только статику и не умеет возвращать
  `index.html` на произвольный путь.

## Acceptance criteria

### Тренажёр (этапы 0–3) — выполнено
- [x] `alembic upgrade head && python -m tools sync` наполняет БД из `content/`
- [x] `python -m tools validate` падает с понятной ошибкой, если эталонный SQL не выполняется
- [x] На главной видны 4 блока с числом задач и прогрессом
- [x] В блоке SQL работают фильтры по сложности, теме, тегу, статусу и текстовый поиск
- [x] На странице задачи можно выполнить запрос и получить результат из PGlite
- [x] Неверный ответ показывает содержательное расхождение, верный — отмечает задачу решённой
- [x] Статус и время сохраняются после перезагрузки страницы
- [x] Светлая и тёмная темы читаемы, выбор темы сохраняется

### Повторение и статистика (этапы 4–5) — выполнено
- [x] Карточки из конспектов попадают в очередь повторения после `sync`
- [x] Оценка назначает дату следующего показа по SM-2, счётчик в шапке уменьшается
- [x] Повторять можно с клавиатуры, ответ скрыт до раскрытия
- [x] Статистика показывает прогресс, календарь занятий, серию дней и слабые темы
- [x] Реализации SM-2 на Python и TypeScript проходят один и тот же набор тест-кейсов

### Теория внутри блоков (этап 7) — выполнено
- [x] Блок открывается хабом с выбором теории и практики, у каждой половины свой прогресс
- [x] В теории блока слева темы из `blocks.yaml`, справа конспект; активная тема подсвечена
- [x] Конспект без темы и конспект с убранной темой попадают в «Без темы» и не теряются
- [x] «Прочитано, дальше →» ставит отметку и открывает следующую тему по порядку списка
- [x] Счётчик прочитанного обновляется на хабе и на дашборде сразу после отметки
- [x] Старые адреса `/theory` и `/theory/:noteSlug` продолжают открываться
- [x] Спойлер `<details>` в конспекте работает в обоих режимах сборки
- [x] Отметки в демо переживают перезагрузку и не требуют ни одного запроса к API

### Публикация (этап 6) — выполнено
- [x] CI прогоняет контент на живом Postgres, тесты обеих частей и сборку
- [x] Демо автоматически деплоится на GitHub Pages при пуше в main
- [x] В демо работают все разделы, включая SQL-раннер, без единого обращения к бэкенду
- [x] Прогресс демо живёт в localStorage и переживает перезагрузку

## Отложено
- Python-раннер на Pyodide и задачи в блоках `stats-ab`, `python`, `ml` (пока там только теория)
- Авторизация и мультипользовательский режим
- Облачный деплой full-stack
- Импорт из внешних источников (Notion, ноутбуки)
- Режим «сессия на 30 минут»: подборка задач под таймер
